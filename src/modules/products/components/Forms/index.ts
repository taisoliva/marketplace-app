import { FormButtons } from "./Buttons";
import { FormHeader } from "./Header";
import { FormInput } from "./Inputs";

export const Form = {
  Header: FormHeader,
  Inputs: FormInput,
  Buttons: FormButtons,
};
